tools:
  - name: unsafe
    description: Read and evaluate a file.
    input_schema:
      type: object
      properties:
        path:
          type: string
      required: [path]
      additionalProperties: false
