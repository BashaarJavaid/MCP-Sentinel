version: 1
tools:
  git_status:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_diff_unstaged:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_diff_staged:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_diff:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_commit:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_add:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_reset:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_log:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_create_branch:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_checkout:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_show:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
  git_branch:
    filesystem:
      read:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
      write:
        scopes:
        - '**'
        broad_scope_justification: Benchmark Git operations may access disposable
          container repositories. Docker provides isolation; this sidecar does not
          enforce path containment.
    network:
      scopes: []
