# Historical compatibility: source support review

This is a source assessment of the **53 completed non-FAF first reports**. The
approved sequence is still running when this packet is prepared. It does not
claim a completed historical stage or human technical acceptance.

`conditions.json` binds each current report to the exact accepted historical
report and source judgment. All 371 ordered findings and 928 registration
surfaces remain identical under the approved eleven volatile exclusions. Labels,
matching keys and original prerequisites remain unchanged. These exposed examples
are regression evidence, not independent accuracy measurements.

## Complete differences

The sealed `../compatibility-first53-assessment-v1/` packet contains complete
ordered report diffs, every added/removed diagnostic occurrence and 60 deduplicated
numbered source files. It accounts for 45,729 warnings and 24,657 unresolved-flow
occurrences in the 53 current reports. Eleven reports equal their references in
full; 32 change warnings only; ten change warnings and unresolved-flow diagnostics.
No other coverage field changes.

| Source | Changed first-report occurrences | Assessment |
| --- | --- | --- |
| Memory-keeper, SearXNG, Fetch MCP, Open WebSearch | 235 added catalog warnings | Unknown helper/configuration/schema bindings remain unknown. Complete flow coverage and findings are unchanged; original caller-specific path/URL qualifications remain. |
| Calculator (five inputs) | 30 removed SDK warnings and 30 removed unresolved flows | The six source locations are `Server.setRequestHandler` registrations with request schema and callback. No sanitizer is inferred. The selected SENT-013 metadata findings and surfaces are unchanged. The corrected scoring maps are copied from accepted v75 evidence; the earlier incomplete evaluation stays failed. |
| Mastra (five inputs) | 150 added catalog warnings | Logger, array/schema and test helper bindings remain unknown; no permission protection is newly credited. Findings and complete coverage are unchanged. |
| Filesystem (four inputs) | Four added `length` catalog warnings | The source-established separator-bounded path guard retains its original ordinary sibling-prefix scope; physical/race limits remain. Findings and complete coverage are unchanged. |
| Mobile (five inputs) | 815 added warnings and 1,760 added unresolved-flow occurrences | Includes 800 Zod-identity warnings plus 15 imported helper/error-member warnings. Conservative schema metadata uncertainty is retained. Selected output-path support is assessed separately below. |
| Kubernetes (five inputs) | Ten added test warnings and 20 removed catalog warnings | Local star exports forward the three tracker types and response schema. No shell sanitizer or runtime safety is inferred. Test filtering callbacks remain unknown. Findings and complete coverage are unchanged. |
| Lighthouse, Taskwarrior, no-bash (eleven inputs) | None | Entire ordered reports equal their accepted source-bound references, including unresolved dispatch and other limitations. |

## Mobile output-path support

The selected condition is Linux output-path containment for screenshot/recording,
with the original valid-extension, existing ordinary parent and functioning
mobile-toolchain prerequisites. It does not establish general schema support,
physical containment or executable integrity.

In the fixed source (`src/server.ts` SHA-256
`aa99170dbf71a75c20985ed191b11758ab8b11bc840e4ee9bdbb046584d24efd`),
recording arguments are declared at lines 703–705, and the handler validates
`output` at 709–711 before constructing `outputPath` at 720 and the argv write
at 722/727. `src/utils.ts` retains resolved allowed roots, separator-relative
containment, and rejection of an outside path. The vulnerable source lacks that
output guard. Mutations and safe controls retain their reviewed transformations.
Full numbered source, including screenshot paths and the utility, is preserved.

The candidate scanner seeds registered handler arguments independently of optional
Zod schema metadata: `typescript_path_flow.py` lines 2434–2477 construct the
`tool:arguments` caller and invoke the proved callback. Unproved `.shape` or
`.describe` metadata emits the retained diagnostic and gives no protection.
Thus the added metadata warnings are not evidence of sanitization and are not
silently excluded. The actual output-path finding/guard qualification, all
registration surfaces and previously unresolved paths remain unchanged. Each
fixed/safe judgment stays bounded to the exact historical source condition.

## Kubernetes export evidence

`kubernetes-export-contexts.json` retains the complete numbered `src/types.ts`
barrel and its three local model modules. The barrel exports the schema and
tracker interfaces by ordinary local star export. The source scanner's reviewed
export forwarding explains the removed unresolved catalog symbols. It does not
change the selected shell findings or their guard qualifications.

## Boundaries

The six Taskwarrior/no-bash source conditions are assessed using complete ordered
report equality and their exact accepted source judgments; provisional null gate
fields in raw runner output remain unchanged. Fixed/safe support is qualified,
not inferred solely from zero findings. All unrelated findings and unresolved
surfaces remain visible with their prior dispositions. No target was imported,
installed or executed; no extra scan or paid call occurred during this assessment.

Final repeat comparison, cleanup, FAF outcome and human review remain pending.
