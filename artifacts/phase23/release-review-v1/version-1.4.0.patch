--- a/README.md
+++ b/README.md
@@ -1336,7 +1336,7 @@
 ```yaml
 repos:
   - repo: https://github.com/BashaarJavaid/MCP-Sentinel
-    rev: v1.3.0
+    rev: v1.4.0
     hooks:
       - id: mcp-sentinel
 ```
--- a/action.yml
+++ b/action.yml
@@ -47,12 +47,12 @@
       with:
         python-version: "3.12"
 
-    - name: Install PortunusMCP Sentinel 1.3.0 from PyPI
+    - name: Install PortunusMCP Sentinel 1.4.0 from PyPI
       shell: bash
       run: >-
         python -m pip install --disable-pip-version-check
         --index-url https://pypi.org/simple
-        portunusmcp-sentinel==1.3.0
+        portunusmcp-sentinel==1.4.0
 
     - name: Run and validate PortunusMCP Sentinel
       id: scan
--- a/docs/github-action.md
+++ b/docs/github-action.md
@@ -1,8 +1,8 @@
 # GitHub Action
 
-The 1.3.0 Action pins `portunusmcp-sentinel==1.3.0`
+The 1.4.0 Action pins `portunusmcp-sentinel==1.4.0`
 package, runs Sentinel, validates SARIF, uploads eligible reports to GitHub code
-scanning, and preserves exit codes. Use `@v1.3.0` for this exact Action release
+scanning, and preserves exit codes. Use `@v1.4.0` for this exact Action release
 or `@v1` for the latest compatible major-version alias.
 
 ## Full scan
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -4,7 +4,7 @@
 
 [project]
 name = "portunusmcp-sentinel"
-version = "1.3.0"
+version = "1.4.0"
 description = "Build-time security scanner for MCP servers"
 readme = "README.md"
 requires-python = ">=3.10,<3.14"
--- a/scripts/smoke_wheel.py
+++ b/scripts/smoke_wheel.py
@@ -17,9 +17,9 @@
 from pathlib import Path
 
 DIST_NAME = "portunusmcp-sentinel"
-VERSION = "1.3.0"
-WHEEL_NAME = "portunusmcp_sentinel-1.3.0-py3-none-any.whl"
-SDIST_NAME = "portunusmcp_sentinel-1.3.0.tar.gz"
+VERSION = "1.4.0"
+WHEEL_NAME = "portunusmcp_sentinel-1.4.0-py3-none-any.whl"
+SDIST_NAME = "portunusmcp_sentinel-1.4.0.tar.gz"
 CLASSIFIERS = {
     "Environment :: Console",
     "Intended Audience :: Developers",
@@ -420,8 +420,8 @@
 
 assert Path(sentinel.__file__).resolve().is_relative_to(Path(sys.prefix).resolve())
 distribution = metadata.distribution("portunusmcp-sentinel")
-assert sentinel.__version__ == "1.3.0"
-assert distribution.version == "1.3.0"
+assert sentinel.__version__ == "1.4.0"
+assert distribution.version == "1.4.0"
 scripts = {
     item.name: item.value
     for item in distribution.entry_points
--- a/src/sentinel/__init__.py
+++ b/src/sentinel/__init__.py
@@ -5,6 +5,6 @@
 try:
     __version__ = version("portunusmcp-sentinel")
 except PackageNotFoundError:  # pragma: no cover - source tree without installation
-    __version__ = "1.3.0"
+    __version__ = "1.4.0"
 
 __all__ = ["__version__"]
--- a/tests/test_cli.py
+++ b/tests/test_cli.py
@@ -43,7 +43,7 @@
 def test_version_and_help() -> None:
     version = runner.invoke(app, ["--version"])
     assert version.exit_code == 0
-    assert version.stdout.strip() == "1.3.0"
+    assert version.stdout.strip() == "1.4.0"
     help_result = runner.invoke(app, ["scan", "--help"], terminal_width=160)
     assert help_result.exit_code == 0
     assert "--static-only" in unstyle(help_result.stdout)
--- a/tests/test_github_action.py
+++ b/tests/test_github_action.py
@@ -358,7 +358,7 @@
     assert metadata["runs"]["steps"][0]["with"]["python-version"] == "3.12"
     assert metadata["runs"]["steps"][1]["run"] == (
         "python -m pip install --disable-pip-version-check "
-        "--index-url https://pypi.org/simple portunusmcp-sentinel==1.3.0"
+        "--index-url https://pypi.org/simple portunusmcp-sentinel==1.4.0"
     )
     uses = [step.get("uses", "") for step in metadata["runs"]["steps"]]
     assert any(value.startswith("actions/setup-python@ece7cb06") for value in uses)
--- a/tests/test_report.py
+++ b/tests/test_report.py
@@ -73,7 +73,7 @@
     )
     report = run_phase1_scan(loaded_config, context, completed_at=NOW).report
     console = render_console(report)
-    assert "PortunusMCP Sentinel 1.3.0" in console
+    assert "PortunusMCP Sentinel 1.4.0" in console
     assert "Target: fixture" in console
     assert "Status: INCOMPLETE" in console
     assert "Findings: 0" in console
--- a/uv.lock
+++ b/uv.lock
@@ -1394,7 +1394,7 @@
 
 [[package]]
 name = "portunusmcp-sentinel"
-version = "1.3.0"
+version = "1.4.0"
 source = { editable = "." }
 dependencies = [
     { name = "jsonschema" },
