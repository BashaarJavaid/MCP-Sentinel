# Approved metadata correction: local engineering passed

**2,748 tests passed / 36 skipped** in one uninterrupted `make check`
run with loopback permission. Lint, formatting, typing, schema validation,
the approved dependency audit, notices and strict documentation all passed.
The focused suite passed **410 tests**, including **51 new metadata checks**.
Coverage: **90.52% combined / 86.58% branch-only**.
See [summary.json](summary.json) for exact source, test, log and dependency bindings.

## What changed

The shared interpreter retains proved literal descriptions, object extension
with right-hand field precedence, and original `.shape` field identities.
Mutation, escape, counterfeit library use, unknown fields and overload ambiguity
remain rejected, including changes after registration. Checks preserve defaults,
original-schema immutability, cross-file locations, all five shared rule paths,
canonical coverage and description poisoning. SENT-016 retains its HTTP boundary.
A source-symbol tuple on the private TypeScript binding carries original fields
to TypeScript coverage and SENT-013. Public Finding/report interfaces and the
existing text-parser contract are unchanged. No dependency or service was added.

New metadata proof is bounded to 256 identities and 64 dependency levels; larger
or unsupported compositions remain unresolved. The known TypeScript historical
scope remains 56 inputs / 112 observations plus 14 first-advisory observations,
bound by the existing exact affected list. Python coverage behavior is unchanged:
the only new coverage branch accepts a private TypeScriptBinding metadata field.

## Retained failed attempts

The original three composed-schema checks failed before the fix. Initial controls
found missing field-description traversal and an incorrect synthetic expectation
for SENT-016's HTTP boundary; both are corrected. A deadline-test import, typing
and formatting issues are retained with their corrections. Three later-mutation
controls exposed stale registration proof; completed discovery now rechecks it.
The first focused command named a nonexistent test file and ran no tests; the
corrected full focused selection passed. Logs remain unmodified.

The initial metadata review remains immutable; the actual implemented patch and
source hashes are retained here. No new corpus scan, target execution or paid call
occurred during development. The earlier candidate's failed full-source gate,
all raw reports, derived support judgments and unrelated option findings remain.

## Next checkpoint

The [next exact 14-observation packet](../candidate-execution-review-v2/README.md)
is prepared, **unapproved and unexecuted**. Synthetic success is not evidence of
full-source advisory recovery. Hosted gates, historical compatibility, durable
offline regression/rejection proof, human technical acceptance and a verified
approved release remain. Phase22 accepted limitations, Proxmox/FAF optimization,
pilot/benchmark deferrals and Phase24/15 are unchanged.
