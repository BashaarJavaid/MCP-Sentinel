diff --git a/AGENTS.md b/AGENTS.md
index cb9468d..f48b61f 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -1,6 +1,25 @@
 # AGENTS.md
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/ARCHITECTURE.md b/ARCHITECTURE.md
index 69c2b1c..7014764 100644
--- a/ARCHITECTURE.md
+++ b/ARCHITECTURE.md
@@ -1,6 +1,25 @@
 # PortunusMCP Sentinel Architecture
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/README.md b/README.md
index 4ee2323..e13b9d9 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,25 @@
 # PortunusMCP Sentinel
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/ROADMAP.md b/ROADMAP.md
index aaf1f81..230fd1e 100644
--- a/ROADMAP.md
+++ b/ROADMAP.md
@@ -1,6 +1,25 @@
 # PortunusMCP Sentinel Roadmap
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/artifacts/phase22/corpus-four-fresh-v2/README.md b/artifacts/phase22/corpus-four-fresh-v2/README.md
index 4c79107..7644a9e 100644
--- a/artifacts/phase22/corpus-four-fresh-v2/README.md
+++ b/artifacts/phase22/corpus-four-fresh-v2/README.md
@@ -1,5 +1,24 @@
 # Four-repository corpus v2
 
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](../integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
 Current source-only packet: exact24-observation evaluation proposal is unapproved; zero observations.
 
 See the [preparation summary](../integration/v43-four-fresh-preparation/summary.md), complete per-repository manifests, source reviews and checkpoint. All original source archives and historical manifests are preserved.
diff --git a/artifacts/phase22/corpus-replacement-v6/README.md b/artifacts/phase22/corpus-replacement-v6/README.md
index 64dd2e9..d5c87de 100644
--- a/artifacts/phase22/corpus-replacement-v6/README.md
+++ b/artifacts/phase22/corpus-replacement-v6/README.md
@@ -1,6 +1,25 @@
 # Replacement-v6: DDG shared-space URL condition
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](../integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/artifacts/phase22/corpus-replacement-v7/README.md b/artifacts/phase22/corpus-replacement-v7/README.md
index 2d5718f..d391d1c 100644
--- a/artifacts/phase22/corpus-replacement-v7/README.md
+++ b/artifacts/phase22/corpus-replacement-v7/README.md
@@ -1,6 +1,25 @@
 # Replacement v7: exposed regression passed; acceptance pending
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](../integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/artifacts/phase22/integration/README.md b/artifacts/phase22/integration/README.md
index 186f5a4..d2e2cb7 100644
--- a/artifacts/phase22/integration/README.md
+++ b/artifacts/phase22/integration/README.md
@@ -1,5 +1,24 @@
 # Integration evidence, batches 1–73 and closeout audit
 
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
 ## Recovery evidence seal 73
 
 [Seal 73](evidence-v73.json) retains **704 members / 18,413,816 raw bytes**,
@@ -37,7 +56,7 @@ Current owning docs and post-seal bindings are directly tracked. The delivery
 readback will be a supplemental post-commit receipt, outside the commit it verifies.
 Phase 22 remains incomplete; this seal records four failed fresh gates, not acceptance.
 
-## Current v45 source recovery: verified, regression approval pending
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/artifacts/phase22/integration/progress.md b/artifacts/phase22/integration/progress.md
index babdc9e..f1aa8b0 100644
--- a/artifacts/phase22/integration/progress.md
+++ b/artifacts/phase22/integration/progress.md
@@ -1,6 +1,25 @@
 # Integration progress — not technical acceptance
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/artifacts/phase22/integration/requirements.md b/artifacts/phase22/integration/requirements.md
index 37d881d..676f91f 100644
--- a/artifacts/phase22/integration/requirements.md
+++ b/artifacts/phase22/integration/requirements.md
@@ -1,6 +1,25 @@
 # Consolidated Phase 22 requirement checklist
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/docs/phase22-corpus-review.md b/docs/phase22-corpus-review.md
index 1056b46..837e94f 100644
--- a/docs/phase22-corpus-review.md
+++ b/docs/phase22-corpus-review.md
@@ -1,6 +1,25 @@
 # Phase 22 independent corpus freeze packet
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/docs/phase22-implementation-status.md b/docs/phase22-implementation-status.md
index a011c66..675d356 100644
--- a/docs/phase22-implementation-status.md
+++ b/docs/phase22-implementation-status.md
@@ -1,6 +1,25 @@
 # Phase 22 implementation status
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/docs/phase22-ssrf-follow-up.md b/docs/phase22-ssrf-follow-up.md
index cfe0996..e1b2dbd 100644
--- a/docs/phase22-ssrf-follow-up.md
+++ b/docs/phase22-ssrf-follow-up.md
@@ -1,6 +1,25 @@
 # Replacement SSRF follow-up
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/docs/phase22-technical.md b/docs/phase22-technical.md
index d53fe1c..68c3728 100644
--- a/docs/phase22-technical.md
+++ b/docs/phase22-technical.md
@@ -1,6 +1,25 @@
 # Phase 22 technical expansion
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/docs/phase22-timeout-policy.md b/docs/phase22-timeout-policy.md
index 58d22d2..6fa3746 100644
--- a/docs/phase22-timeout-policy.md
+++ b/docs/phase22-timeout-policy.md
@@ -1,6 +1,25 @@
 # Phase 22 static timing policy
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
diff --git a/docs/rules.md b/docs/rules.md
index 1f9bbd7..fb6a754 100644
--- a/docs/rules.md
+++ b/docs/rules.md
@@ -1,6 +1,25 @@
 # PortunusMCP Sentinel rule catalog
 
-## Current v45 source recovery: verified, regression approval pending
+## Current v46 regression timeout: budget closed
+
+The approved 205-observation sequence at frozen `17b4784` stopped on its first
+FAF vulnerable input at the 300-second whole-input maximum: **one incomplete,
+zero completed reports, 204 unstarted closed, zero remaining**. Cleanup passed.
+No finding, guard, negative-path or ordered-repeat result is available; corrected
+four-repository and prior-language compatibility gates remain unestablished.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v46-four-source-regression/summary.md) retains the failure and an **unapproved single
+300-second CPU-sampling diagnostic** on the same FAF input. No retry, profile,
+target execution or paid call has occurred. All original fresh failures and
+historical source-bound passes remain unchanged. Engineering retains `17b4784`:
+2,380 tests / 36 skips locally and in all 12 hosted suites, 29 normal CI jobs and
+docs passed; combined coverage 89.98%, branch-only 85.85%. The audit retains all
+233 requirements. **Phase 22 remains incomplete**; no failure/limitation or human
+technical acceptance is inferred. Git stays 312/1,040 incomplete; paid benchmark
+and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state change, release, outreach or Phase 23.
+
+
+## Historical v45 source recovery: verified, regression approval pending
 
 Source recovery at `17b4784` passes **2,380 tests / 36 skips** locally and in all
 12 hosted suites; all 29 normal CI jobs and docs pass. Local combined branch-enabled
