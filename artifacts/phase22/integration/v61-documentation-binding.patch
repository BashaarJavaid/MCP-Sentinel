diff --git a/AGENTS.md b/AGENTS.md
index f765a68..9a7ff6f 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -1,6 +1,34 @@
 # AGENTS.md
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/ARCHITECTURE.md b/ARCHITECTURE.md
index f9dbe39..76561eb 100644
--- a/ARCHITECTURE.md
+++ b/ARCHITECTURE.md
@@ -1,6 +1,34 @@
 # PortunusMCP Sentinel Architecture
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/README.md b/README.md
index b88003b..46a8a6b 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,34 @@
 # PortunusMCP Sentinel
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/ROADMAP.md b/ROADMAP.md
index 03add64..a877092 100644
--- a/ROADMAP.md
+++ b/ROADMAP.md
@@ -1,6 +1,34 @@
 # PortunusMCP Sentinel Roadmap
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/artifacts/phase22/corpus-four-fresh-v2/README.md b/artifacts/phase22/corpus-four-fresh-v2/README.md
index f57fdbe..db6159f 100644
--- a/artifacts/phase22/corpus-four-fresh-v2/README.md
+++ b/artifacts/phase22/corpus-four-fresh-v2/README.md
@@ -1,6 +1,34 @@
 # Four-repository corpus v2
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/artifacts/phase22/corpus-replacement-v6/README.md b/artifacts/phase22/corpus-replacement-v6/README.md
index f077bc4..a79c7ed 100644
--- a/artifacts/phase22/corpus-replacement-v6/README.md
+++ b/artifacts/phase22/corpus-replacement-v6/README.md
@@ -1,6 +1,34 @@
 # Replacement-v6: DDG shared-space URL condition
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/artifacts/phase22/corpus-replacement-v7/README.md b/artifacts/phase22/corpus-replacement-v7/README.md
index 5077249..830b65f 100644
--- a/artifacts/phase22/corpus-replacement-v7/README.md
+++ b/artifacts/phase22/corpus-replacement-v7/README.md
@@ -1,6 +1,34 @@
 # Replacement v7: exposed regression passed; acceptance pending
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/artifacts/phase22/integration/README.md b/artifacts/phase22/integration/README.md
index 02070a0..10c74b1 100644
--- a/artifacts/phase22/integration/README.md
+++ b/artifacts/phase22/integration/README.md
@@ -128,7 +128,35 @@ paths/symlinks, verify archive/member hashes and stop on unexplained conflicts.
 The v73 log correction remains preserved. Post-seal documentation and delivery
 readback are supplemental. **Phase 22 remains incomplete.**
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/artifacts/phase22/integration/progress.md b/artifacts/phase22/integration/progress.md
index 4dc0947..5c8cff9 100644
--- a/artifacts/phase22/integration/progress.md
+++ b/artifacts/phase22/integration/progress.md
@@ -1,6 +1,34 @@
 # Integration progress — not technical acceptance
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/artifacts/phase22/integration/requirements.md b/artifacts/phase22/integration/requirements.md
index 5e9d911..15530b4 100644
--- a/artifacts/phase22/integration/requirements.md
+++ b/artifacts/phase22/integration/requirements.md
@@ -1,6 +1,34 @@
 # Consolidated Phase 22 requirement checklist
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/docs/phase22-corpus-review.md b/docs/phase22-corpus-review.md
index a17ad6e..e175ac5 100644
--- a/docs/phase22-corpus-review.md
+++ b/docs/phase22-corpus-review.md
@@ -1,6 +1,34 @@
 # Phase 22 independent corpus freeze packet
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/docs/phase22-implementation-status.md b/docs/phase22-implementation-status.md
index 53a376d..6a34476 100644
--- a/docs/phase22-implementation-status.md
+++ b/docs/phase22-implementation-status.md
@@ -1,6 +1,34 @@
 # Phase 22 implementation status
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/docs/phase22-ssrf-follow-up.md b/docs/phase22-ssrf-follow-up.md
index 3ba098e..652320c 100644
--- a/docs/phase22-ssrf-follow-up.md
+++ b/docs/phase22-ssrf-follow-up.md
@@ -1,6 +1,34 @@
 # Replacement SSRF follow-up
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/docs/phase22-technical.md b/docs/phase22-technical.md
index 8c0bdfe..fc813e8 100644
--- a/docs/phase22-technical.md
+++ b/docs/phase22-technical.md
@@ -1,6 +1,34 @@
 # Phase 22 technical expansion
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/docs/phase22-timeout-policy.md b/docs/phase22-timeout-policy.md
index 3711233..b09899b 100644
--- a/docs/phase22-timeout-policy.md
+++ b/docs/phase22-timeout-policy.md
@@ -1,6 +1,34 @@
 # Phase 22 static timing policy
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
diff --git a/docs/rules.md b/docs/rules.md
index e48bf90..eae081a 100644
--- a/docs/rules.md
+++ b/docs/rules.md
@@ -1,6 +1,34 @@
 # PortunusMCP Sentinel rule catalog
 
-## Current v60 thirty-minute regression: timed out, budget closed
+## Current v61 thirty-minute profile: source-only decision pending
+
+The approved FAF profile at frozen **`7bf4c6e`** timed out after
+**1800.090669 seconds**, retaining **four verified worker identities
+and 432,756 partial samples**. Cleanup passed; no report exists and the
+one-use budget is closed. Registration discovery appears in **99.93–99.93%**
+of each worker's retained samples. These overlapping partial shares do not prove
+native speedup, removable-cost fraction, detection or compatibility.
+
+`v61-faf-long-timeout-sampling/optimization-proposal.json` prepares **one unapproved
+source-only attempt** to reuse completed TypeScript tool discovery across the
+existing workers and coverage. Preserve ordered warnings and source-node identity;
+all rule-specific analysis still executes. Full synthetic/engineering verification
+is required. No implementation, corpus/profile/retry/comparator, target execution,
+paid call, resource change or timeout revision is included in current approval.
+The shared 1800-second policy remains in place.
+
+All **335 requirements (89 original + 246 added)** and every earlier failed gate
+remain. Product/tests retain verified `7bf4c6e`: **2,403 tests / 36 skips** locally
+and in all 12 hosted suites, 29 normal CI jobs and docs passed. Combined coverage
+is 89.98%, branch-only 85.86%; six zero-call replays and approved runtime bindings
+remain compatible. This continuation adds profile assessment and docs/package checks.
+
+**Phase 22 remains incomplete**, pending current-source gates, explicit human
+technical acceptance and accepted closeout. Git stays 312/1,040 incomplete;
+paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v60 thirty-minute regression: timed out, budget closed
 
 The approved 205-observation sequence at frozen **`7bf4c6e`** stopped on the first
 FAF vulnerable input after **1800.021672 seconds**. **One incomplete,
