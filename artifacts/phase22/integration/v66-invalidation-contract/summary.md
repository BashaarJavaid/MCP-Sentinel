# Private invalidation contract verified; regression approval pending

**The expressly revised private-set contract and one source-only attempt pass synthetic and full local/hosted engineering at `4145d35`. Phase 22 remains incomplete. No current-source Phase 22 corpus input, profile or paid call ran in this continuation.**

The exact decision `approved` binds [authorization](authorization.json) to the delivered v65 proposal SHA-256 `9e3d3691a0c7efa584d7cfcab24a5ad4cc2ab6b89aebaea7ee699840356650a7`. [One attempt is consumed and closed](optimization-budget-closed.json). The actual candidate is `4145d3559c5300a5003ca8e3368c8da49fb51f91`, source SHA-256 `41dd31a167c454bd9db3d720ce75ce6287312ce0e7799bbda1b2e59c61f77ea6`, harness SHA-256 `9bfc9229b7ec7d23580c44f2306edb9274bc1d2a3c2261d01db0445b8b480add`. The [clean frozen checkout](freeze.json) is `/private/tmp/mcp-phase22-frozen-4145d35`. The existing locked environment, workers and 1800-second shared deadline remain unchanged.

## Exact revision, retained failure and source proof

The approved domain is the shipped interpreter and its source-supported flows. Private builtin invalidation sets grow monotonically within an arm, and completed arm-set references do not escape. [Production proof](production-domain-proof.json) inventories every direct reader/writer, indirect attribute-string check, all seven shipped flow classes and source-supported entry points. It covers all statement kinds, constructors, imports, callbacks, guards, error paths and the HTTP whole-flow deep copy. That copy's memo shares source/program/report objects, never the private invalidation set. Unknown target calls still receive conservative object/receiver invalidation; no target behavior is excluded or executed.

[Source proof](source-proof.json) establishes byte-for-byte reapplication of the retained v65 candidate. Reversing only the If accumulator edit restores the complete baseline file. Each feasible arm still starts from an independent copy of the original baseline. The first completed arm now supplies the union accumulator; subsequent arms union into it. Zero feasible arms retain the original fresh-copy fallback. No guard, statement, environment merge, per-arm copy, rule analysis or cleanup changes. The only other engineering changes are the source-bound regression guard and changelog; all **303 engineering inputs** are bound.

The **v65 strict-equivalence attempt remains failed and unresolved**. Its injected alias callback retains the first arm's set: the candidate later adds the other arm's keys to that retained alias, while the baseline does not. Its shrinking callback clears an arm: baseline final invalidations retain `["baseline"]`, candidate final invalidations are `[]`. [The exact complete old deltas](equivalence.json) are reproduced unchanged. The explicit user decision prospectively classifies **only those two named injected scanner callbacks** outside this private interpreter domain. It does not retroactively pass v65 or accept any other failed gate. Real-target reachability of those injected scanner behaviors remains unestablished. No additional counterexample is excluded.

## Verification

- **256 ordinary synthetic comparisons** retain complete flow, Value, environment, original input and relevant alias state across initial-set sizes, feasible/absent/terminating/nested arms and preexisting aliases. The ordinary two-arm case avoids one accumulator copy and its first update; per-arm analysis still runs.
- **766 complete production-flow comparisons across 263 tests** cover actual class/import/SDK/factory and recovery paths. [The comparison](production-flow-equivalence.json) includes every interpreter/program/state field and injected test callback closure state.
- **252 real-flow boundary comparisons** cover all seven flow classes, errors, deadline exceptions, interruptions and preexisting aliases. Both feasible unknown object calls remain invalidated. See [boundary evidence](production-boundaries.json).
- **125 complete ordered discovery comparisons across 157 tests** preserve bindings, warnings and module option effects. [Six complete baseline-versus-candidate reports](five-rule-report-equivalence.json) cover Python, TypeScript and mixed-workspace serial/parallel results, every static result field and ordered findings/warnings/coverage. All five SENT-012 through016 rules are explicitly selected; the original six four-worker results remain separately retained with their corrected scope wording. The existing ten worker tests pass; only the established eleven volatile names are excluded.
- [Five mutation guard controls](guard-validation.json) reject direct and local-alias removal/export and reflective access. The candidate itself passes the committed guard.

These are source equivalence and avoided-operation checks, **not measured native speedup, removable-cost fraction, FAF completion or current-source corpus compatibility**. A future input may still exceed 1800 seconds.

[Every first check failure is retained](check-failure-assessment.json). The original full-flow helper copied a test callback without rebinding its saved bound method, allowing reference execution to mutate the original flow. A retained negative control reproduces the mismatch with identical baseline methods; correcting callback closure copies restores independent comparisons. The first full local run had 2,415 passes, 36 skips and four sandbox-denied loopback socket bindings; the complete collection was rerun at identical source with authorized loopback access. The first run remains failed and retained. Other helper failures concerned the tests import root, sandbox uv-cache access, an oversized synthetic parsing deadline and a primitive fixture that did not meet the existing object-invalidation predicate. Their exact helpers/logs and successful corrections remain. No candidate source change or failure exclusion was used to fix them.

## Actual engineering and runtime bindings

[Local checks](local-checks.json) and [hosted audit](../v66-candidate-quality-audit/packet.json) bind **2,419 passed / 36 skipped** locally and in all **12 Linux/macOS/Windows × Python 3.10–3.13 suites**. All **29 normal CI jobs** and documentation pass in [34808261686](https://github.com/BashaarJavaid/MCP-Sentinel/actions/runs/34808261686) / [34808261635](https://github.com/BashaarJavaid/MCP-Sentinel/actions/runs/34808261635). Actual hosted merge `90eb6e874eed01d15d23decb2980fd91238aae26` has complete tree `d78c8dff1394348242d1332df8439f45011450dc`, equal to the candidate. Combined statement/branch coverage is **90.08%**; branch-only is **85.92%**.

Configured Ruff/format and whole-scope strict mypy, locked dependencies/advisories, schemas, notices, offline artifacts, strict docs and source-bound wheel/sdist checks pass. Hosted evidence covers installed-wheel platforms, Docker, network isolation, Action and hooks. Four optional corpus jobs remain skipped. Normal historical Phase 20 reproduction uses its pinned `8824014` scanner and is not a new current-source corpus observation.

[Six entire production requests](../v66-production-capture-revalidation/packet.json) regenerate and checked-replay with identical request hashes/fingerprints and **zero live model calls**. [Runtime reuse](compatibility.json) binds all 19 unchanged components and the approved Git image. The owned demo retains 20 attempts and 14 findings. Historical spend remains two paid calls totaling **$0.071799**; no new paid call occurred and no Git campaign reopened.

## Separate, unapproved regression

[Exact proposal](evaluation-proposal.json), SHA-256 **`a925c4e396b328e2c49f84669fb710f09c58f3490956d868e70a58786ae3c2bf`**, is prepared, **unapproved and unexecuted**. [Launcher binding](launcher-binding.json), [rubric](scoring-rubric.json) and unchanged evaluator preserve all original source records, labels, reports, prerequisites and order.

| Scope | Observations |
| --- | ---: |
| FAF, no-bash, Lightning Python and Engram | 24 |
| Earlier TypeScript compatibility | 94 |
| Earlier Python compatibility | 87 |
| Total | **205 on 110 inputs / 95 entire ordered pairs** |

Both language schedules remain necessary because the earlier shared recovery and discovery changes still lack completed corpus compatibility. Fifteen Python development inputs occur once; language subsets are not relabeled whole Linux batches. All selected manifest/source bindings validate without scanning or executing targets.

One serial local macOS sequence uses the frozen scanner: **1800 seconds maximum per native/whole input**, **15 seconds cleanup**, **120 seconds informational target**, **6275 minutes outer cap** (104 hours 35 minutes), 6274 minutes internal stop. These are worst-case ceilings, not duration estimates. There are **zero retries, profiles, comparators, new repositories, target executions or paid calls**. The existing eight deadline and condition/process-supervisor controls pass. Missing-approval preflight rejects before creating an evaluation token or output directory.

Infrastructure, identity, execution/timeout, schema, cleanup or ordered-repeat failure closes every unused observation. Detection/support failures remain results for full source assessment after collecting other approved inputs. All 95 entire ordered pairs must agree after only the established eleven volatile exclusions; no sorting or diagnostic omission. No source tuning, retry or substitution during collection.

All original named gates remain: caller path to FAF's actual read; checked return to no-bash's actual write; actual HTTPX route and terminal literal link-local rejection in Lightning; initial home guard and actual Engram manifest write with unresolved reconstructed-path containment retained. Every qualifier requires actual source support. Quiet unsupported paths fail. Lexical/physical, initial-destination and resolved-copy qualifiers do not imply broad runtime safety. All findings, diagnostics and surfaces and every prior-language delta require source assessment.

## Audit, historical failures and acceptance

[All 365 prior rows are retained](prior-row-preservation.json); the [audit](audit.json) has **376 requirements: 89 original + 287 added**. Only V65 delivery and revision decision change to passed using actual receipts, with complete previous rows preserved. V65 equivalence remains unresolved. Original rows retain 84 passed, two user-deferred, two proposed limitations and one unresolved human acceptance. Before supplemental delivery, added rows have 263 passed, six historical closure proposals and 18 unresolved. No technical acceptance is inferred.

All six native failures at `17b4784`, `6e4fd67`, `a36f696`, `dc73715`, `7bf4c6e` and `1948bf9` retain one incomplete input, no report and 204 unstarted closed each. Four earlier valid worker profiles retain 86,659 / 79,307 / 78,769 / 432,756 partial samples at their actual sources. The separate `1948bf9` parent-only profile retains 148,764 partial samples and no worker/report. The invalid stale-root preparation remains invalid; both singleton failures and the strict accumulator failure remain failed. Different partial sampling coverage does not establish speedup.

Original four-source fresh evaluation at `2e0efb2` retains 24 complete observations and 12 equal pairs, but **all four gates failed**: FAF/Lightning/Engram missed their named vulnerable sinks; no-bash alerted on fixed/control inputs. Later observations are exposed regression. TS94/47 pairs at `2e0efb2`, Python87/36 pairs at `8c62567`, and whole25 development reuse plus45+45 Linux at `1f3f72f` remain source-bound, including the Meta operator erratum. Memory-keeper false alerts, DDG fixed-send support failure and Python-language erratum, Lighthouse misses, and held-out10 complete/10 unsupported/5 incomplete with zero hits among four completed vulnerable inputs out of ten vulnerable total remain visible.

The six historical proposed closures (V14-RETENTION, V21-SEQUENCE, V21-FRESH-PROPOSAL, V25-EXPOSED-REGRESSION, V27-REGRESSION, V39-EVALUATION), R66/R88 limitations and separate human R84 acceptance remain unaccepted. Same-agent assessment is not independent human validation, training-data novelty or broad accuracy proof.

Git stays **312/1,040 incomplete**, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. **Phase 22 remains incomplete** pending separately approved current-source regression and actual source/gate assessment, then explicit human technical acceptance and verified accepted closeout. No merge, ready-state change, release, outreach or Phase 23.
