diff --git a/AGENTS.md b/AGENTS.md
index cfc63f1..17b10dd 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -1,6 +1,32 @@
 # AGENTS.md
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/ARCHITECTURE.md b/ARCHITECTURE.md
index aa7b9fc..4549afa 100644
--- a/ARCHITECTURE.md
+++ b/ARCHITECTURE.md
@@ -1,6 +1,32 @@
 # PortunusMCP Sentinel Architecture
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/README.md b/README.md
index 0332f2b..12e40b6 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,32 @@
 # PortunusMCP Sentinel
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/ROADMAP.md b/ROADMAP.md
index 4456342..d7bdad4 100644
--- a/ROADMAP.md
+++ b/ROADMAP.md
@@ -1,6 +1,32 @@
 # PortunusMCP Sentinel Roadmap
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/artifacts/phase22/corpus-four-fresh-v2/README.md b/artifacts/phase22/corpus-four-fresh-v2/README.md
index 45bb4e2..a56abae 100644
--- a/artifacts/phase22/corpus-four-fresh-v2/README.md
+++ b/artifacts/phase22/corpus-four-fresh-v2/README.md
@@ -1,6 +1,32 @@
 # Four-repository corpus v2
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/artifacts/phase22/corpus-replacement-v6/README.md b/artifacts/phase22/corpus-replacement-v6/README.md
index d0b48e2..639a1e1 100644
--- a/artifacts/phase22/corpus-replacement-v6/README.md
+++ b/artifacts/phase22/corpus-replacement-v6/README.md
@@ -1,6 +1,32 @@
 # Replacement-v6: DDG shared-space URL condition
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/artifacts/phase22/corpus-replacement-v7/README.md b/artifacts/phase22/corpus-replacement-v7/README.md
index 8a486b4..b1edf0a 100644
--- a/artifacts/phase22/corpus-replacement-v7/README.md
+++ b/artifacts/phase22/corpus-replacement-v7/README.md
@@ -1,6 +1,32 @@
 # Replacement v7: exposed regression passed; acceptance pending
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/artifacts/phase22/integration/README.md b/artifacts/phase22/integration/README.md
index 0c53b1c..cfc6786 100644
--- a/artifacts/phase22/integration/README.md
+++ b/artifacts/phase22/integration/README.md
@@ -103,7 +103,33 @@ paths/symlinks, verify archive/member hashes and stop on unexplained conflicts.
 The v73 log correction remains preserved. Post-seal documentation and delivery
 readback are supplemental. **Phase 22 remains incomplete.**
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/artifacts/phase22/integration/progress.md b/artifacts/phase22/integration/progress.md
index 7f16ebe..67465e9 100644
--- a/artifacts/phase22/integration/progress.md
+++ b/artifacts/phase22/integration/progress.md
@@ -1,6 +1,32 @@
 # Integration progress — not technical acceptance
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/artifacts/phase22/integration/requirements.md b/artifacts/phase22/integration/requirements.md
index e19edc0..4f11d51 100644
--- a/artifacts/phase22/integration/requirements.md
+++ b/artifacts/phase22/integration/requirements.md
@@ -1,6 +1,32 @@
 # Consolidated Phase 22 requirement checklist
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/docs/phase22-corpus-review.md b/docs/phase22-corpus-review.md
index d8cd154..da8d29c 100644
--- a/docs/phase22-corpus-review.md
+++ b/docs/phase22-corpus-review.md
@@ -1,6 +1,32 @@
 # Phase 22 independent corpus freeze packet
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/docs/phase22-implementation-status.md b/docs/phase22-implementation-status.md
index 23e5de3..a969e27 100644
--- a/docs/phase22-implementation-status.md
+++ b/docs/phase22-implementation-status.md
@@ -1,6 +1,32 @@
 # Phase 22 implementation status
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/docs/phase22-ssrf-follow-up.md b/docs/phase22-ssrf-follow-up.md
index 0db3eff..ca18cb2 100644
--- a/docs/phase22-ssrf-follow-up.md
+++ b/docs/phase22-ssrf-follow-up.md
@@ -1,6 +1,32 @@
 # Replacement SSRF follow-up
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/docs/phase22-technical.md b/docs/phase22-technical.md
index cc9631b..f89c415 100644
--- a/docs/phase22-technical.md
+++ b/docs/phase22-technical.md
@@ -1,6 +1,32 @@
 # Phase 22 technical expansion
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
diff --git a/docs/phase22-timeout-policy.md b/docs/phase22-timeout-policy.md
index 936ef15..bd964ce 100644
--- a/docs/phase22-timeout-policy.md
+++ b/docs/phase22-timeout-policy.md
@@ -1,6 +1,32 @@
 # Phase 22 static timing policy
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
@@ -932,8 +958,9 @@ remain deferred, Phase 21 incomplete and Phase 24/15 unchanged.
 
 On 2026-09-13, the user asked why the scan's duration mattered and instructed
 “yes do that.” after the proposal to revise the timeout policy and evaluate
-completed results at a longer supported limit. The initial implementation uses
-**1,800 seconds (30 minutes)** uniformly for deterministic static analysis.
+completed results at a longer supported limit, then explicitly confirmed the
+**30-minute** preference. The implementation uses **1,800 seconds** uniformly
+for deterministic static analysis.
 **120 seconds remains informational**, with no separate failure at that target.
 This is a prospective policy revision, not a performance improvement or acceptance
 of any previous incomplete measurement. Engineering verification and exact new
diff --git a/docs/rules.md b/docs/rules.md
index 32ebffc..74f1f39 100644
--- a/docs/rules.md
+++ b/docs/rules.md
@@ -1,6 +1,32 @@
 # PortunusMCP Sentinel rule catalog
 
-## Current v58 credential regression timeout: budget closed
+## Current v59 timeout policy: engineering passed, evaluation pending
+
+The user confirmed **30 minutes per input**. The shared static deadline is now
+1,800 seconds at **`7bf4c6e`**, source `40c194dcd47a152f78c9b9dc57b40e5e77026f6052035841c0af63b83f234af6`;
+120 seconds remains informational. Source proof establishes one product AST
+literal change; detector logic and cleanup are unchanged from `dc73715`.
+Local and all 12 hosted suites pass **2,403 tests / 36 skips**; all 29 normal jobs
+and docs pass in 34784085204 / 34784085193. Six production requests replay with
+zero paid calls. Actual engineering and runtime bindings are retained.
+
+`v59-timeout-policy/evaluation-proposal.json` is **unapproved and unexecuted**:
+205 exposed observations, 110 input records, 95 ordered pairs; 30-minute input
+ceiling, 15-second cleanup and 6,275-minute outer cap. This is a worst-case cap,
+not a duration estimate. All original conditions, support requirements and
+11 volatile exclusions remain. No retry, profile, comparator, new repository,
+target execution or paid call. The pending v58 diagnostic is preserved unexecuted.
+
+All four earlier native timeouts, partial/invalid profiles, singleton failures
+and original fresh failures remain source-bound and unaccepted. No speedup,
+current-source compatibility or new unseen-source discrimination is established.
+The audit retains **323 rows (89 original + 234 added)**, including all 314 prior
+rows and six unaccepted historical closure proposals. **Phase 22 is incomplete**:
+actual regression, source assessment, explicit human acceptance and closeout remain.
+Git stays 312/1,040 incomplete; paid benchmark/pilots deferred, Phase 21 incomplete
+and Phase 24/15 unchanged. No merge, ready-state, release, outreach or Phase 23.
+
+## Historical v58 credential regression timeout: budget closed
 
 The approved 205-observation sequence at frozen **`dc73715`** stopped on the first
 FAF vulnerable input at **300.005621 seconds** whole-input time.
