MCP handlers can hide unsafe filesystem, process-option, URL and credential flows behind helpers, factories, clients and middleware. This integration follows supported bindings, adds SENT-014–016, expands workspace/registration coverage and preserves canonical evidence, unresolved flows and ordered runtime campaigns. It builds on the containment and description-poisoning parent increments.

The current correction at **6eb482c** follows low-level MCP factory handlers through `McpServer.server` and ordinary else branches. Supported local loopback guards retain narrow evidence, including a default-only qualifier when bypass requires an actual environment option set to true. Mutation, unknown effects and unrelated checks do not establish protection. Broader SSRF candidates remain visible.

| Final exposed native family | Inputs per batch × batches | Vulnerable matches per batch | Matching negative alerts per batch |
| --- | --- | --- | --- |
| fetch-mcp | 5 × 2, all complete | 2/2 | 0 |
| open-webSearch | 5 × 2, all complete | 2/2 | 0 |
| SearXNG | 5 × 2, all complete | 2/2 | 2 fixed false alerts |

**SearXNG's correction gate still fails.** Its full source loses the narrow guard qualifier and flags both fixed variants. The public-IPv4 control also retains a broad candidate, separately assessed outside that exact public-input condition; this does not waive the fixed false alerts. The MCP dispatch surface remains unresolved. All ten authorized SearXNG observations are consumed. Entire ordered repeats match except documented volatile fields, JSON/SARIF validate, and every new/changed finding or diagnostic has a source assessment. Original frozen native/comparator 0/2 results remain unchanged; these fixes are exposed regression work, not fresh generalization evidence.

The bounded performance investigation used one helper-context counter profile. Fewer than 0.6% of calls repeated the recorded signature, all changing branch bindings; no safely reusable result was established. No optimization, native timing comparison or further full retry occurred. The retained whole Linux sequence 34435283462 still completes 15/25 development inputs, times out on all ten Meta cases and skips both historical batches. The separate prior diagnostic retains four native and two profiled timeouts. Timing remains unmet.

Final-source engineering verification passes: **2,169 tests / 36 skips / no expected failures** locally and in each of 12 hosted quality suites, with 89.66% local and 89.65–89.67% hosted branch coverage. All 29 normal jobs and docs pass ([CI34492040679](https://github.com/BashaarJavaid/MCP-Sentinel/actions/runs/34492040679), [docs34492040778](https://github.com/BashaarJavaid/MCP-Sentinel/actions/runs/34492040778)). Actual wheel/sdist source members match Git. Packaging smoke, Docker replay, network isolation, dependencies, Ruff/format/strict mypy, schemas, notices and offline artifacts pass. Intermediate socket and dependency-download failures are preserved with their actual source. Pinned historical reproduction jobs do not establish current-source timing.

Both approved production review requests were regenerated and replayed without live calls. Approved Git runtime evidence retains exact compatibility: 13 incomplete campaigns,312 tested,728 remaining. Original held-out 10 completed/10 unsupported/5 incomplete and0/4 completed-vulnerable hits remain historical evidence. No completed Git coverage or unchanged whole-corpus scanner claim is made.

The [current status](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/docs/phase22-implementation-status.md) and [evidence/restore index](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/README.md) preserve all 89 requirements: **69 passed, two user-deferred, 18 unresolved**, with evidence batches 1–41. The [next SearXNG diagnostic/final-run proposal](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v12-next-searxng-proposal.json) is prepared but unapproved. The prior draft guard-contract checkpoint was premature and explicitly corrected; the actual next approval is an additional bounded measurement budget.

**Phase 22 remains incomplete; final human acceptance is not requested.** Pilots and the full paid benchmark remain user-deferred; Phase 21 is incomplete and Phase 24/15 gates are unchanged. No additional paid call, benchmark target execution, optional hosted measurement dispatch, full retry, runner/gate change, merge, release, outreach or next phase occurred. The final documentation/evidence-only commit preserves all tested code/test/workflow/package inputs and uses the explicitly permitted CI-skip convention; its docs check is local, with no new hosted pass claimed for that later commit.
