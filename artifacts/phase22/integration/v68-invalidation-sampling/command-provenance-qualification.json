{
  "direct_command_vectors": "In command-provenance.json, recovery helper command vectors identify the retained helper and locked interpreter. Full terminal transcripts were not retained, so the -I flag and exact invocation spelling in those reconstructed vectors are not independently verified. Use individual result receipts and retained helper source for actual effects, and cleanup receipts for exact Docker command vectors/stdout. The original/resumed launcher vectors are retained in the failure receipt and launch proposal.",
  "paid_calls": 0
}
