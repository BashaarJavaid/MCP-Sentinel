# Current-source profile assessed; literal serialization proposal pending

**One approved FAF profile at `4145d35` timed out after 1800.013224667 seconds. It retains 148,001 verified parent samples, no report, verified cleanup and a closed one-use budget. Phase 22 remains incomplete.**

The user approved the [exact v67 diagnostic](../v67-invalidation-regression/diagnostic-proposal.json), SHA `9c20d0ce8365dc18188854c8417598ab8936243b5405e179d887df3479f022fb`, delivered at `c60b7a858497c355326044b959d9f6ad8d755a80`. [Authorization](../v67-invalidation-regression/diagnostic-authorization.json), original and restored preflights, [attempt](attempt.json), [execution](execution.json) and [closed budget](budget-closed.json) bind the sole actual input. Later “continue” messages do not approve another optimization or accept Phase 22.

Scanner `4145d3559c5300a5003ca8e3368c8da49fb51f91`, source `41dd31a167c454bd9db3d720ce75ce6287312ce0e7799bbda1b2e59c61f77ea6`, harness `9bfc9229b7ec7d23580c44f2306edb9274bc1d2a3c2261d01db0445b8b480add`, lock `c34da413d4761ca42f243bc41a10afd8961a98daa1f3a236d37fe03720743b2b`. The frozen checkout remains `/private/tmp/mcp-phase22-frozen-4145d35`. The exact source, rules-only configuration, parent/up-to-four-worker layout, 100Hz CPU timer, 1024-frame cap and 15-second atomic periodic snapshots match approval. No target code was imported or executed, no model transport ran, and no worker/resource/deadline policy changed.

## Actual profile and source assessment

[Validation](validation.json) checks every retained sample/frame reference, source/identity/configuration binding, actual incomplete harness result and budget. Whole input was **1800.013224667 seconds**, including cleanup **1805.202977458 seconds**; cleanup used 5.190 seconds of its separate 15-second allowance. The supervisor forced remaining-group termination, return -9 (shell247). [Owned-work](owned-work.json) confirms no remaining owned process or Sentinel container. The harness retained incomplete/null reason at 1,798,583ms and `observation.log` reports incomplete. No JSON/SARIF report exists; findings, warnings, surfaces, named detection, fixed/control support and ordered repeats are **unknown**, not zero.

There is **one periodic parent snapshot**, with identity verified before timers and input access. Legacy worker-named fields describe the parent here: `final_worker_snapshot=false` and `signal_and_timer_state_restored=false`. These do not confirm final snapshot completion or timer restoration. No temporary snapshot file or worker identity/snapshot survived. The cause of incomplete finalization beyond the recorded forced teardown is unestablished. Missing worker snapshots do not prove workers never launched. The prior v64 final/restoration-true snapshot is a distinct historical result and is not copied into this assessment.

[Source assessment](source-assessment.json), [all frame contexts](frame-contexts.json), [source hashes](frame-source-bindings.json) and [complete attribution](sample-attribution.json) retain every frame and denominator. The periodic snapshot records process CPU1691.265082seconds, sampling start1.77432seconds, retained interval1689.490762seconds and maximum observed depth202. CPU time differs from whole-input wall time and final work is unobserved.

| Retained parent attribution | Samples | Share |
| --- | ---: | ---: |
| Registration discovery, inclusive | 147,799 | 99.8635% |
| TypeScript branch merge, inclusive | See complete attribution | 24.3255% |
| JSON encoder leaves, all callers | 8,004 | 5.4081% |
| JSON encoder leaves at literal serialization | 5,697 | 3.8493% |
| Merge equal-value predicate leaves | 5,551 | 3.7507% |
| Shared combine entry leaves, all recorded lines | 6,354 | 4.2932% |

Source places147,798registration samples under `factory_tools` line85 invoking module initialization. The largest individual retained leaf site is `statement` line685, the later-arm invalidation union (11,483samples); line676 is per-arm baseline copying (7,889), line576 recursive block statement dispatch (7,566), and line669 initial baseline copying (3,422). All rows remain available, including calls outside these selected sites. These are sampled line attributions, not operation counts or cardinalities. Inclusive rows overlap with leaf rows and must not be added as savings. Signal/GIL/native-code delivery, snapshot serialization/thread overhead, partial intervals and absent-worker data limit inference. There is no cross-version speedup, removable-cost or 1800-second completion result.

At the frozen source, completed TypeScript tool discovery is computed in the parent before existing worker dispatch and reused by consumers/coverage. `RegistrationFlow.expression` calls the full `TypeScriptPathFlow.expression`, then retains metadata. Every non-null/non-undefined literal visit serializes `node["L"]` with `json.dumps(..., sort_keys=True)` at line1057 and constructs a fresh Value before updating literal/condition state. The current source-range cache already relies on program syntax immutability and retains node/file identities in a bounded OrderedDict. It does not cache literal serialization. HTTP flow deepcopy deliberately preserves the program and AST graph. The proposed cache must still prove generated-node lifetimes, error behavior, identity-sensitive consumers and reconstruction before implementation can pass.

The v66 private monotone invalidation-domain revision remains unchanged. Removing later-arm unions or per-arm/environment copies would require further isolation/mutation work; the proposed change targets pure serialization only. No branch, function, module, rule or coverage traversal is bypassed. Registration remains expensive, and even eliminating the proposed sampled leaf cost may leave the scan unable to finish. Cache hits, misses, eviction rates and overhead are unmeasured.

## File recovery and authorized Docker cleanup

The [initial launch failure](../v67-invalidation-regression/launch-preflight-failure.json) occurred in read-only preflight when a manifest disappeared, **before output creation, token consumption or any input attempt**. Original preflight had passed;1485tracked deletions were then inventoried in the active checkout, with more in retained worktrees. The first commentary incorrectly inferred input start from launcher startup and was promptly corrected. A premature read-only assertion that an attempt file existed also failed before helper creation. Both errors remain preserved.

[Recovery assessment](recovery-assessment.json) binds restoration of **25,941 tracked files across eight affected worktrees**, including24,737APFS clones and1204Git-blob writes; **six missing worktree links**, **7,667 expanded evidence files** and **two protected user documents**. All95sealed archives and members were verified. Existing files were not overwritten; HEADs, indexes and historical evidence versions were preserved. The two user documents came from original recorded creation text and matched their prior SHA-256 before exclusive creation; no historical command was executed and no private session log is published. All nine protected user documents verify. The frozen4145d35checkout was unaffected. [Restored preflight](../v67-invalidation-regression/restored-launch-preflight.json) reverified source,303engineering files, configuration, protected documents and absent conflicting work before the same still-unused profile approval ran once.

**The disappearance cause remains unknown.** The user suggested insufficient disk space; roughly35GB was available during inspection, and no evidence establishes ENOSPC or a cleanup process as the cause.

The user explicitly requested freeing unneeded Docker storage. Two exact immutable/reclaimable Sentinel build-cache entries were selected; fine-grained prune returned0B and both remained, which is retained as zero cache reclamation. A public-image guard initially assumed empty tags and stopped before mutation; Docker returned digest-qualified tags, so the corrected guard requires each exact known public repository+digest and no container use. All five selected unused public base images were then removed without force. Project images, approved Git/demo runtime image IDs, **all seven containers and65volumes** are preserved.

Docker's rounded image total decreased from9.151GB to6.261GB (**2.89GB logical reduction**). The immediate sparse-disk physical reduction was0 and remains in the original receipt. A later read records **493,813,760 physical bytes reclaimed**, with **35,511,771,136 bytes available** on the host. Logical image storage is not physical disk space; other filesystem activity also affects host availability. No global prune, project data deletion or daemon configuration change occurred. Read-only inventory/format errors and both helper versions remain recorded.

## Exact next source-only proposal — unapproved and unimplemented

[Proposal](optimization-proposal.json), SHA-256 **`fa4ffc3760d4b85b4f8236eec5c7b28d979ece894515bb1495046dd1db108cf6`**: **one source-only attempt** to reuse exact serialized literal strings within the existing TypeScriptProgram, using the existing stdlib OrderedDict identity-cache pattern, maximum4096entries with LRU eviction. Retain node/file objects; new reconstructed programs start empty and no integer-ID cache crosses processes. Cache only at the current literal expression site, without changed JSON options or error caching. Source targets are `typescript_discovery.py` and `typescript_path_flow.py`, plus required scanner-owned tests/release notes.

Every expression still creates its original fresh Value, calls `program.literal`, rebuilds literal/condition metadata and executes subclass registration/security bookkeeping. Full Value, environment, flow, relevant alias/interning state and ordered output must agree. Before editing, trace every consumer, immutable/generated-node lifetime, string-key identity use, deepcopy and IPC path. Cold/warm/>4096-entry eviction, errors, deadline interruption, source-node identity, all seven flow classes, ordered discovery/warnings/options, all five rules and full serial/parallel Python/TypeScript/mixed synthetic reports are required. Synthetic operation counts must show avoided repeated serialization without bypassing state updates. No exposed corpus may become a synthetic fixture.

If equivalence prerequisites fail, preserve the counterexample, close this one attempt and restore exact4145d35source. Do not silently narrow the existing contract or switch targets. If they pass, complete full local/hosted engineering and six complete zero-call production request revalidations under the handoff, freeze the tested candidate, then prepare affected regression for separate approval. This proposal includes **zero corpus observations, profiles, retries, comparators, new repositories, runtime campaigns, target executions or paid calls**. No worker/resource/deadline revision, dependency, merge, ready-state, release, outreach or Phase23. The old preliminary v64 literal-key option was never selected/approved/implemented; this is a new current-source proposal.

## Engineering, complete audit and acceptance

[Compatible reuse](compatible-reuse.json) verifies all303engineering inputs against tested `4145d35`: **2,419passed/36skipped** locally and all12hosted suites; **29normal CI jobs and docs passed** in [34808261686](https://github.com/BashaarJavaid/MCP-Sentinel/actions/runs/34808261686) / [34808261635](https://github.com/BashaarJavaid/MCP-Sentinel/actions/runs/34808261635). Actual merge `90eb6e874eed01d15d23decb2980fd91238aae26`, tree `d78c8dff1394348242d1332df8439f45011450dc`, matches candidate tree. Combined coverage90.08%, branch-only85.92%. Six full production requests retain identical zero-call replay and approved runtime/image bindings. This continuation adds profile/recovery assessment and final docs/package checks, not a new hosted code pass or production capture.

The [audit](audit.json) retains **all383prior rows**, full prior versions for updated V67 delivery/profile-decision rows, all original metadata and source-specific interpretations; eight new rows produce **391total =89original+302added**. Original dispositions remain84passed,2deferred,2proposed limitations and1unresolved human acceptance. Added dispositions before supplemental delivery:277passed,6unaccepted historical closure proposals,19unresolved. Draft delivery is separately verified after sealing; it does not accept technical limitations.

All seven native timeouts remain bound to their scanners, each1incomplete/0reports/204unstarted closed; six valid partial profiles retain separate source/process denominators. Invalid v50 attribution and V49 preparation, both singleton failures and original v65 strict invalidation failure remain unresolved. The explicit v66 private-domain revision does not retroactively pass v65. The six unaccepted closure IDs remain V14-RETENTION, V21-SEQUENCE, V21-FRESH-PROPOSAL, V25-EXPOSED-REGRESSION, V27-REGRESSION and V39-EVALUATION.

The original four-repository batch stays24complete/12equal pairs/**all four gates failed at2e0efb2**: FAF, Lightning Python and Engram missed named sinks; no-bash falsely alerted on fixed/control inputs. Unsupported quiet negatives fail. Historical TS94/47pairs, Python87/36pairs, whole25development reuse and45+45Linux retain actual source bindings; no subsets become whole Linux batches. Original held-out10complete/10unsupported/5incomplete, zero hits among four completed vulnerable inputs out of ten vulnerable total, memory-keeper/DDG/Lighthouse failures and all errata remain in the complete audit and [prior review](../v67-invalidation-regression/summary.md). Same-agent source review is not independent validation, broad accuracy or runtime safety proof.

Git remains **312/1040incomplete**,728deferred; paid396-request benchmark/pilots remain user-deferred, Phase21incomplete and Phase24/15unchanged. Historical two paid calls remain **$0.071799total**, zero added. **Phase22is incomplete:** current-source detection/compatibility gates or explicit applicable dispositions, then separate human technical acceptance and verified accepted closeout remain. No merge, ready-state change, release, outreach or Phase23 is authorized.
