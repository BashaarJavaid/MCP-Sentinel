diff --git a/AGENTS.md b/AGENTS.md
index bc64c45..9520cd2 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -1,6 +1,37 @@
 # AGENTS.md
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/ARCHITECTURE.md b/ARCHITECTURE.md
index b8534db..ec98a81 100644
--- a/ARCHITECTURE.md
+++ b/ARCHITECTURE.md
@@ -1,6 +1,37 @@
 # PortunusMCP Sentinel Architecture
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/README.md b/README.md
index b5218f4..0d29106 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,37 @@
 # PortunusMCP Sentinel
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/ROADMAP.md b/ROADMAP.md
index dc6213d..84ba855 100644
--- a/ROADMAP.md
+++ b/ROADMAP.md
@@ -1,6 +1,37 @@
 # PortunusMCP Sentinel Roadmap
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/artifacts/phase22/corpus-four-fresh-v2/README.md b/artifacts/phase22/corpus-four-fresh-v2/README.md
index c1c5a33..be5c1a8 100644
--- a/artifacts/phase22/corpus-four-fresh-v2/README.md
+++ b/artifacts/phase22/corpus-four-fresh-v2/README.md
@@ -1,6 +1,37 @@
 # Four-repository corpus v2
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/artifacts/phase22/corpus-replacement-v6/README.md b/artifacts/phase22/corpus-replacement-v6/README.md
index c4eb977..10901cf 100644
--- a/artifacts/phase22/corpus-replacement-v6/README.md
+++ b/artifacts/phase22/corpus-replacement-v6/README.md
@@ -1,6 +1,37 @@
 # Replacement-v6: DDG shared-space URL condition
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/artifacts/phase22/corpus-replacement-v7/README.md b/artifacts/phase22/corpus-replacement-v7/README.md
index 63abeeb..47f5197 100644
--- a/artifacts/phase22/corpus-replacement-v7/README.md
+++ b/artifacts/phase22/corpus-replacement-v7/README.md
@@ -1,6 +1,37 @@
 # Replacement v7: exposed regression passed; acceptance pending
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/artifacts/phase22/corpus-unseen-v1/README.md b/artifacts/phase22/corpus-unseen-v1/README.md
index a69d708..f3cc73c 100644
--- a/artifacts/phase22/corpus-unseen-v1/README.md
+++ b/artifacts/phase22/corpus-unseen-v1/README.md
@@ -1,3 +1,42 @@
+# Two-repository source packet: first-frozen results retained, exposed regression pending
+
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+The original V80 run at `cec0322` completed 12 observations and six equal ordered pairs. Taskwarrior passed its narrow shell condition; Proxmox missed both vulnerable reads and had four unsupported negatives. The preceding V78 metadata failure and its closed budget remain recorded.
+
+These six unchanged inputs are now exposed. The corrected `884d376` candidate is included in the separately approvable V86 regression proposal; no corrected-source corpus run has occurred. See [current proposal](../integration/v86-regression-preparation/summary.md) and [original complete assessment](../integration/v80-unseen-results/final-assessment/assessment.json). Conditions, prerequisites, licenses, manifest and source archives are unchanged. No paid call or target execution.
+
+## Historical preparation text before any observation
+
 # Two-repository unseen-source packet
 
 Six immutable vulnerable/fixed/safe records at scanner `cec0322`, proposed twice. No approval or scans yet.
diff --git a/artifacts/phase22/integration/README.md b/artifacts/phase22/integration/README.md
index ec1eb5a..96dc4c0 100644
--- a/artifacts/phase22/integration/README.md
+++ b/artifacts/phase22/integration/README.md
@@ -1,5 +1,36 @@
 # Integration evidence, batches 1–102 and closeout audit
 
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
 ## Current unseen-review evidence seal 102
 
 [Seal 102](evidence-v102.json) retains **203 members / 17,842,249 raw bytes**;
@@ -13,7 +44,7 @@ Restore after seals 1–101 in numeric order into separate staging, rejecting un
 checking every member hash and stopping on unexplained conflicts. Post-seal documentation and
 actual draft delivery receipts are supplemental. **Phase 22 remains incomplete; zero new paid calls.**
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/artifacts/phase22/integration/progress.md b/artifacts/phase22/integration/progress.md
index c1a3297..17c8285 100644
--- a/artifacts/phase22/integration/progress.md
+++ b/artifacts/phase22/integration/progress.md
@@ -1,6 +1,37 @@
 # Integration progress — not technical acceptance
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/artifacts/phase22/integration/requirements.md b/artifacts/phase22/integration/requirements.md
index c9fd2b7..62811c6 100644
--- a/artifacts/phase22/integration/requirements.md
+++ b/artifacts/phase22/integration/requirements.md
@@ -1,6 +1,37 @@
 # Consolidated Phase 22 requirement checklist
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/docs/phase22-corpus-review.md b/docs/phase22-corpus-review.md
index cc49e57..2ca9d08 100644
--- a/docs/phase22-corpus-review.md
+++ b/docs/phase22-corpus-review.md
@@ -1,6 +1,37 @@
 # Phase 22 independent corpus freeze packet
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/docs/phase22-implementation-status.md b/docs/phase22-implementation-status.md
index c506138..be9ae3c 100644
--- a/docs/phase22-implementation-status.md
+++ b/docs/phase22-implementation-status.md
@@ -1,6 +1,37 @@
 # Phase 22 implementation status
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/docs/phase22-ssrf-follow-up.md b/docs/phase22-ssrf-follow-up.md
index b22218e..53308d0 100644
--- a/docs/phase22-ssrf-follow-up.md
+++ b/docs/phase22-ssrf-follow-up.md
@@ -1,6 +1,37 @@
 # Replacement SSRF follow-up
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/docs/phase22-technical.md b/docs/phase22-technical.md
index a5c0773..83c6716 100644
--- a/docs/phase22-technical.md
+++ b/docs/phase22-technical.md
@@ -1,6 +1,37 @@
 # Phase 22 technical expansion
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/docs/phase22-timeout-policy.md b/docs/phase22-timeout-policy.md
index 429c845..005f9c2 100644
--- a/docs/phase22-timeout-policy.md
+++ b/docs/phase22-timeout-policy.md
@@ -1,6 +1,37 @@
 # Phase 22 static timing policy
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
diff --git a/docs/rules.md b/docs/rules.md
index 5663aac..9243f67 100644
--- a/docs/rules.md
+++ b/docs/rules.md
@@ -1,6 +1,37 @@
 # PortunusMCP Sentinel rule catalog
 
-## Current v81 technical review: unseen check complete, one failed gate
+## Current v86 correctness recovery: engineering passed, regression approval pending
+
+The approved Python module-dispatch and conservative global receiver correction
+is engineering-verified at **`884d376`**: **2,553 tests / 36 skips** locally and in
+all 12 hosted suites, **29 normal CI jobs plus docs**, combined coverage **90.26%**,
+branch-only **86.20%**. Six zero-call production requests and 19 approved
+runtime component bindings pass. The failed `8f0639f` candidate, its 45-pass
+interrupted local suite and cancelled CI remain preserved. Candidate `1ee96cb`
+passed its 2,549/36 local suite, then failed a class-call mutation control; its CI
+was cancelled. The final correction removes that unproved class-call exception. No speedup is claimed.
+
+The exact **111-observation exposed regression is unapproved and unexecuted**:
+12 Proxmox/Taskwarrior, 12 Lightning/Engram and 87 earlier Python observations,
+63 input records across 10 existing repositories, 48 entire ordered pairs and
+15 development singles. Normal
+1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker limits
+remain; the 3,455-minute outer cap is a worst-case bound, not an estimate.
+No new repository, retry, profile, comparator, target execution or paid call.
+
+All **463 requirements** retain previous states and evidence; **26 limitation
+proposals remain unaccepted**. Original V80 Proxmox misses and unsupported
+negatives remain failed. Actual corrected-source support is pending regression.
+Historical V73/V75 results remain bound to `cec0322`; ordinary FAF completion
+within 1,800 seconds remains unestablished. V68 is unapproved and unimplemented.
+
+**Phase 22 remains incomplete**, pending actual affected regression, explicit human
+technical acceptance and verified accepted-closeout delivery. Git stays 312/1,040
+incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete,
+Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
+No merge, ready-state change, release, outreach or Phase 23 is authorized.
+
+## Historical v81 technical review: unseen check complete, one failed gate
 
 The separately approved V80 check completed **12 observations / six equal ordered
 pairs** at **`cec0322`**, using normal 1,800s input / 10s Semgrep / 15s cleanup and
