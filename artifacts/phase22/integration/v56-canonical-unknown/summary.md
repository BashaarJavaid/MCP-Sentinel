# Canonical-empty bypass failed eviction equivalence; source restored

**The approved canonical `UNKNOWN_VALUE` bypass passed the two retained ordinary identity cases, then failed its cache-eviction environment equivalence prerequisite. One attempt consumed, zero remaining; exact tested `a36f696` source restored. Phase 22 remains incomplete.**

The user's `approved` decision binds [authorization](authorization.json) to proposal SHA-256 `2ff8d58ed4b5f082ffffc6f05fd31ada841dce7cf2bd165347dc627d251f77b9`, delivered at `8df485e`. The two-line [candidate patch](candidate.patch), complete candidate/baseline sources, [synthetic results](synthetic-validation.json), [assessment](assessment.json) and [closed budget](optimization-budget-closed.json) are retained. No corpus observation, profile, comparator, retry, target execution or paid call occurred.

The functional check first reproduces the broad bypass's original contained-false failure and confirms that the narrow candidate preserves baseline environments for both retained ordinary cases. It then fills the established 4,096-entry LRU with one tainted value and 4,095 other entries. Baseline caching of `UNKNOWN_VALUE` evicts the tainted entry; the narrow bypass preserves it. A later equal-but-distinct tainted input therefore returns different object identities between implementations. `CredentialFlow.merge` tests marker identity only: the baseline substitutes `UNKNOWN_VALUE`, while the narrow candidate preserves the marker. **The environment differs; every other structurally compared flow field agrees, and inputs remain unchanged.** Cache filling here is a synthetic functional check, not a performance measurement.

This is a deliberately adversarial internal contained-false credential-marker state. **Real-target reachability and finding impact remain unestablished.** Source guard constructors establish contained-true markers. Known credential guard/assignment/option consumers require containment, while helper and launch state forwarding also retains these markers and needs downstream review. The complete-state gate did not authorize disregarding this case after failure. The candidate was preserved and reverted immediately. The broader metadata matrix, affected/full local suites and hosted engineering were not run for the rejected candidate; no engineering pass is claimed for it.

The [new proposal](optimization-proposal.json), SHA-256 `12eea305c03328d6872587a051c090daf6f2d1bb83e1d0a622e3c3a92af92921`, is an **unapproved prospective scope revision**, not another claim of baseline-preserving bypass. It requests one joint source-only attempt with two specified edits:

1. Let `CredentialFlow.merge` reuse the first marker when every branch holds the same object **or a completely equal Value**, preserving all unequal-marker, containment, HTTP-selection and other merge paths.
2. After validating that correction, add the exact canonical `UNKNOWN_VALUE` singleton/no-key bypass; keep `_combine`, its existing LRU and all other inputs unchanged.

The proposed contract intentionally changes the previous behavior for equal-but-distinct contained-false markers that formerly fell through to `UNKNOWN_VALUE`. That specific baseline delta must stay visible and every downstream difference must be assessed. Uncontained markers must not establish absent-caller or operator-opt-in guard facts. Any unrelated semantic change fails the attempt. Against a preserved **credential-correction-only reference**, the bypass must then satisfy full Value/environment/flow equality with no exclusions, including cold, warm, saturated and evicted caches and all relevant Python/TypeScript metadata and controls. Both older failures retain their original sources and outcomes.

If prerequisites pass, complete affected/full local and all hosted engineering, static/package/schema/lock/docs checks, zero-call production/runtime compatibility and freeze before preparing a separate both-language regression proposal. The joint attempt stops on its first verified unrelated semantic, bypass-equivalence, guard-fact or avoided-work failure. No additional target or attempt is included. Bounds remain **zero corpus observations, profiles, retries, comparators, new repositories, target executions, runtime campaigns or paid calls**. Neither edit is implemented or approved now.

Canonical unknown frequency remains unmeasured. The retained 7.40–7.64% combine/cache-call leaf share is motivation, not removable cost or a promised speedup. No completion within 300 seconds is guaranteed.

All original measurements remain source-bound: three native timeouts at `17b4784` (300.002159 seconds), `6e4fd67` (300.004111) and `a36f696` (300.000481), each one incomplete plus 204 unstarted closed. Partial profiles retain 86,659 samples at `17b4784`, 79,307 at `6e4fd67`, and 78,769 at `a36f696`; the latest reached 300.007176 seconds with verified worker identities and cleanup. The invalid stale-root v50 attempt remains unusable, V49-DIAGNOSTIC-PREPARATION unresolved, and both singleton optimization failures remain closed and unaccepted. The earlier whole-flow comparison helper defect and reconciliation label assertion failure remain preserved with their separate corrections.

The original four-repository fresh batch remains 24 completed observations, 12 equal pairs and all four gates failed at `2e0efb2`: FAF actual read unsupported; no-bash one vulnerable hit plus two fixed/control false alerts per batch; Lightning client-get and Engram named manifest-write unresolved. Later corrections remain exposed, not unseen-source discrimination.

Restored source retains `a36f696` engineering: **2,386 tests / 36 skips locally and in all 12 hosted suites**, all 29 normal CI jobs and docs passed in [34767417083](https://github.com/BashaarJavaid/MCP-Sentinel/actions/runs/34767417083) / [34767417081](https://github.com/BashaarJavaid/MCP-Sentinel/actions/runs/34767417081), combined coverage 89.98% and branch-only 85.85%. All 302 engineering files, six unchanged zero-call production requests and approved Docker/Git runtime bindings retain their sources. Final docs/packages verify this reverted-source delivery; no new hosted code pass is claimed. Earlier file/worktree/evidence restoration and selective Docker cleanup remain retained.

The [audit](audit.json) preserves all 290 prior rows and adds seven: **297 total, 89 original plus 208 added**. Original dispositions remain 84 passed, two user-deferred, two proposed limitations and one human acceptance pending. Added rows have 188 passed, six historical closure proposals and 14 unresolved before supplemental delivery verification. Neither failed equivalence gate is relabeled as passed or accepted.

TS94 at `2e0efb2`, Python87 at `8c62567`, whole 25 development reuse and 45+45 Linux at `1f3f72f` retain their actual passes and Meta operator erratum; partial language subsets are never pooled into new whole batches. Original memory-keeper false alerts, DDG's initially unsupported fixed send and wrong TypeScript label despite Python execution, Lighthouse misses, and the held-out 10 complete / 10 unsupported / five incomplete with zero hits among four completed vulnerable inputs out of ten vulnerable total remain unchanged. Same-agent review establishes neither independent human validation, training-data novelty, broad accuracy nor runtime safety.

Git remains **312/1,040 incomplete**, 728 deferred; paid benchmark and pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. Historical two paid calls cost $0.071799; this continuation adds zero. Explicit human technical acceptance and accepted closeout remain pending. No merge, ready-state change, release, outreach or Phase 23 is authorized.
