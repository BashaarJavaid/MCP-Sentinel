# Phase 22: failed regression retained, literal metadata correction verified

## Current v89 literal metadata correction: engineering passed, regression approval pending

The approved V87 regression at **`884d376`** completed **111 observations / 48 equal
entire ordered pairs**, with verified cleanup and zero remaining budget. Taskwarrior,
Lightning, Engram and prior Python conditions retain their exact qualifications,
including two Meta nominal-fixed operator errata. **Proxmox still fails: two missed
vulnerable reads and four unsupported negatives.** All 3,959 findings, 343,791
diagnostic occurrences, 5,686 surfaces and complete ordered differences are assessed.
Ten Meta reports retain 268 assessed changed occurrences; original failures remain.

The authorized literal metadata correction is engineering-verified at **`5142a3f`**:
**2,596 tests / 36 skips** locally and all 12 hosted suites, **29 normal jobs and docs**,
coverage **90.28% combined / 86.23% branch-only**. Six zero-call production
requests and 19 approved runtime bindings pass. Literal dictionary copies preserve
dispatch only with proved builtin, mapping and mutation/escape boundaries. Initial
deletion and typing failures remain preserved. No current candidate corpus scan or
speedup is claimed.

The exact **six-observation Proxmox regression is unapproved and unexecuted**:
three unchanged exposed inputs, vulnerable/fixed/safe then repeat, three entire ordered
pairs. Normal **1,800-second input / 10-second Semgrep / 15-second cleanup / four-worker**
limits remain; **200 minutes plus 15 seconds outer cleanup** is a worst-case cap.
Source-only inventory proves the other 105 V87 observations cannot reach the changed
metadata code; their original `884d376` report/source bindings remain, with no new scans.

All **479 requirements** and **28 unaccepted limitation proposals** remain. Ordinary
FAF completion within 1,800 seconds is unestablished; V68 stays unapproved/unimplemented.
**Phase 22 is incomplete**, pending the remaining actual gate, updated complete review,
explicit human technical acceptance and verified accepted-closeout delivery. Git remains
312/1,040 incomplete, 728 deferred; paid benchmark/pilots deferred, Phase 21 incomplete
and Phase 24/15 unchanged. Historical spend **$0.071799; zero new paid calls**.
No merge, ready-state change, release, outreach or Phase 23 is authorized.


The previous recognizer rejected every call nested in Tool metadata. Proxmox's firewall schema uses `dict(_SCOPE_PROPS)`, causing the entire dispatch table—including storage upload—to remain unsupported. The correction accepts only source-proved literal dictionary copies and unpack reads. Dynamic/custom mappings, shadowed constructors, aliases, mutations, namespace changes and ambiguous routing remain conservative. Full detection and negative support on actual Proxmox source still require the separately approved run.

V87 completed in 3825.620672 seconds; maximum whole input 151.592829 seconds. Shared-host source assessment occurred during collection, so these are observed completion times, not isolated benchmarks. All 101 unchanged reports reuse prior judgments only after complete ordered/source identity verification. Ten Meta reports retain the same finding keys/order/counts while cached AuthManager evidence becomes conservative: direct environment credential evidence remains, and the two original nominal-fixed operator errata remain alerts. The first preflight and two Meta assessment helper failures are retained with actual corrections; no additional observation was used.

The first V88 synthetic control run had one failure and 141 passes: direct `del dict` was absent from ordinary bindings. The correction adds explicit rejection. Initial typing errors were corrected by annotating the protected mutable set. The final source passes 146 module controls, 513 focused checks and fresh full local/hosted engineering (CI34922354713, docs34922354718). The previous `8f0639f` and `1ee96cb` failures, interrupted/cancelled engineering and actual full local pass remain unchanged.

| Proposed order | Inputs | Observations | Ordered pairs |
| --- | ---: | ---: | ---: |
| Proxmox vulnerable, fixed, safe; then the same order repeated | 3 | 6 | 3 |

The immutable manifest retains both original repositories for source-only validation; the numerical schedule selects only the three Proxmox records. Current source-path analysis covers all 63 preceding records and excludes the other 105 V87 observations from modified code. Those measurements remain at `884d376`, with exact source/report/assessment identities and the new execution-path proof; no rerun or new-source measurement is claimed. Older TypeScript, no-bash and FAF results likewise retain their actual measured sources and exact qualifications.

Both vulnerable observations must detect the named local read through actual dispatch; fixed/safe observations require supported terminal-root guard/value/sink qualification without matching alerts. A quiet unsupported negative fails. The original stable operator upload directory, witnesses, ordinary readable files and no-race/physical-identity qualifications remain. No remote exfiltration, successful API interaction, authentication/TLS or general filesystem safety is asserted. All unmatched findings and unresolved surfaces must be assessed. Only the existing eleven volatile fields are excluded from complete ordered repeat comparisons.

One attempt per slot; cancellation or identity/infrastructure/execution/timeout/schema/cleanup/ordered-repeat failure stops and closes unused budget. Detection misses, false alerts and unsupported negatives remain failed outcomes while collection continues. No retries, profiles, comparators, new repositories, target execution, performance attempts or paid calls. The 200-minute outer cap includes 180 input minutes, 90 seconds total input cleanup, 1,050 seconds setup/identity/selfcheck allowance and 60 seconds finalization; outer cleanup has its separate 15-second limit. This is a worst-case cap, not an estimate.

[Exact proposal](evaluation-proposal.json), SHA-256 `2b5bf9605374aafc4afd1265fe9baef163e1fe4b8020fefa4f3c03a72306f027`; [completed V87 assessment](../v87-regression-results/final-assessment/assessment.json), [source correction and compatibility proof](../v88-literal-metadata-correction/source-assessment.json), [local engineering](../v88-literal-metadata-correction/local-checks.json), [hosted audit](../v88-candidate-quality-audit/packet.json), [complete 479-row audit](audit.json), [all unaccepted limitations](unaccepted-limitations.md).

After the approved remaining regression and complete source assessment, deliver a new full technical acceptance packet. Obtain separate explicit human technical acceptance, then actually deliver and verify accepted closeout on existing OPEN DRAFT PR37. Numerical approval is neither technical acceptance nor closeout.
