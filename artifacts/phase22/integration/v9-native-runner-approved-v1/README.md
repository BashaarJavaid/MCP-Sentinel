# Authorized final Linux sequence

The current continuation authorizes this one standard Linux sequence at immutable scanner `7555a9d`. Native and end-to-end 120-second deadlines and existing condition gates remain unchanged. Development25 must pass before historical45; the first history must pass before the second. Stop after at most115 inputs and retain all failures. No retry, larger runner, pooling, paid call or deadline waiver is authorized.

Gate scripts and source condition assessments are unchanged from the previous runner proposal. The separate preflight must validate current report compatibility before dispatch. This is not a measured pass or final human Phase22 acceptance.
