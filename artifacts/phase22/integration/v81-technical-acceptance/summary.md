# Phase 22 technical acceptance review after the unseen check

**The requested new evaluation is complete. Taskwarrior passes its narrow shell condition; Proxmox fails vulnerable detection and negative support. Phase 22 remains incomplete pending explicit human acceptance of the enumerated limitations and verified accepted closeout.**

| Separate scope at frozen `cec0322` | Actual outcome | Qualification |
| --- | --- | --- |
| V73 four-repository exposed experiment | 24 complete, 12 equal ordered pairs, four narrow gates pass | Uncapped FAF took 2,297.758–2,735.386s; normal 1,800s completion remains unestablished |
| V75 earlier-language compatibility | 181 complete, 83 equal ordered pairs; 94 TypeScript / 87 Python | Normal policy; two Meta nominal-fixed operator errata remain |
| V78 initial unseen launch | One incomplete before analysis, zero reports, eleven unstarted closed | Proposal omitted three optional null defaults; failure and spent budget retained |
| V80 corrected unseen launch | 12 complete, six equal ordered pairs; one of two narrow repository gates passes | Normal input 1,800s / Semgrep 10s / cleanup 15s / four workers; actual whole-input 3.284–5.731s |

Taskwarrior: `mark_task_done.identifier` reaches the vulnerable shell template, detected on both passes. Fixed `execFileSync` passes it as argv without a shell; all four fixed/safe observations have supported narrow discrimination. Aggregate dispatch and Zod parse metadata remain unresolved. The two vulnerable reports each retain an additional source-supported `add_task` shell candidate outside the named condition.

Proxmox: the module list and nested `get_tools()` loops build `_DISPATCH`; `call_tool` looks up a module and invokes its handler. Current Python registration discovery recovers literal selector branches, not that computed table. All six reports mark this handler unsupported. The vulnerable local upload read is missed twice, and all four quiet negatives lack read/guard support. The upstream fixed helper is source-guarded under the original stable-root/no-race prerequisites, but that does not establish detector support. No tuning, hidden support scan, target execution or automatic waiver occurred. An extension would require conservative module/handler/name and mutation/alias controls; this review explicitly proposes leaving the form unsupported for closeout.

All four findings, 504 diagnostics, 12 surfaces, 18 source contexts and 14 complete ordered comparisons are source-assessed. Every report/JSON/SARIF/configuration/runtime binding and budget is retained. Six repeats are equal after only the existing eleven volatile exclusions. Fixed/safe differ only in record display name; related variants and repeats are correlated, not independent discoveries. The Proxmox static schema-field description warning is retained and source-qualified; zero warnings/flows do not imply coverage.

Sources retain their initial pre-curation freeze, full upstream pairs and recorded-project novelty. Taskwarrior has a full MIT license and requires the declared SDK, available zod and configured task executable. Proxmox has declaration-only MIT evidence and needs a stable operator upload directory/readable regular files; no successful exfiltration or physical-use guarantee. Same-agent source review is not independent human validation or model-training-data novelty.

Unchanged engineering remains actual `cec0322`: 2,450 tests / 36 skips locally and all 12 hosted suites, 29 normal CI jobs plus docs, 90.11% combined / 85.97% branch coverage, 303 engineering input hashes, six zero-call production requests and 19 runtime components. This continuation adds affected docs/package and helper checks, not a new hosted code pass. Measured source, delivered revision and workflow checkout retain separate identities.

[Complete 448-row audit](audit.json), [all proposed limitations](acceptance-limits.md), [exact acceptance proposal](acceptance-proposal.json), [V80 complete assessment](../v80-unseen-results/final-assessment/assessment.json), [source diagnosis](../v80-unseen-results/final-assessment/decisive-source-review.json), [completion verification](../v80-unseen-results/completion-verification.json), [V78 stopped failure](../v78-unseen-results/stop-assessment.json), and [prior V76 review](../v76-technical-acceptance/summary.md).

All 440 prior rows persist, with eight added. Twenty-six limitation rows remain proposed, including the new Proxmox failure and initial metadata failure, original held-out/fresh failures, seven native timeouts, partial/invalid profiles, singleton/strict-equivalence failures, ordinary FAF timing and leaving V68 unopened. No clean broad accuracy estimate, speedup or independent review is claimed. V73/V75 caller-specific DDG CGNAT, two Meta errata, calculator outside-condition findings and fourteen Git-copy physical/identity qualifications remain unchanged.

Git remains 312/1,040 incomplete, 728 deferred; the 396-request paid benchmark and pilots remain deferred/nonblocking under the revised scope. Phase 21 is incomplete and Phase 24/15 unchanged. Historical spend $0.071799; zero new paid calls. Explicit acceptance must name this delivered packet and all proposed limitations, followed by actual verified accepted-closeout delivery. No merge, ready-state change, release, outreach or Phase 23 is authorized.
