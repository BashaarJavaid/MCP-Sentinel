diff --git a/AGENTS.md b/AGENTS.md
index b3c0fe5..83e0501 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -1,6 +1,26 @@
 # AGENTS.md
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/ARCHITECTURE.md b/ARCHITECTURE.md
index 2d9104d..a0582ac 100644
--- a/ARCHITECTURE.md
+++ b/ARCHITECTURE.md
@@ -1,6 +1,26 @@
 # PortunusMCP Sentinel Architecture
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/README.md b/README.md
index 5e73c89..da8ccca 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,26 @@
 # PortunusMCP Sentinel
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/ROADMAP.md b/ROADMAP.md
index 790008e..5a7e188 100644
--- a/ROADMAP.md
+++ b/ROADMAP.md
@@ -1,6 +1,26 @@
 # PortunusMCP Sentinel Roadmap
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/artifacts/phase22/corpus-four-fresh-v2/README.md b/artifacts/phase22/corpus-four-fresh-v2/README.md
index f6248be..bdfc35e 100644
--- a/artifacts/phase22/corpus-four-fresh-v2/README.md
+++ b/artifacts/phase22/corpus-four-fresh-v2/README.md
@@ -1,6 +1,26 @@
 # Four-repository corpus v2
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](../integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/artifacts/phase22/corpus-replacement-v6/README.md b/artifacts/phase22/corpus-replacement-v6/README.md
index e6052c4..349b93c 100644
--- a/artifacts/phase22/corpus-replacement-v6/README.md
+++ b/artifacts/phase22/corpus-replacement-v6/README.md
@@ -1,6 +1,26 @@
 # Replacement-v6: DDG shared-space URL condition
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](../integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/artifacts/phase22/corpus-replacement-v7/README.md b/artifacts/phase22/corpus-replacement-v7/README.md
index e231dae..f4e7f3e 100644
--- a/artifacts/phase22/corpus-replacement-v7/README.md
+++ b/artifacts/phase22/corpus-replacement-v7/README.md
@@ -1,6 +1,26 @@
 # Replacement v7: exposed regression passed; acceptance pending
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](../integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/artifacts/phase22/integration/README.md b/artifacts/phase22/integration/README.md
index 048a369..da64654 100644
--- a/artifacts/phase22/integration/README.md
+++ b/artifacts/phase22/integration/README.md
@@ -1,5 +1,25 @@
 # Integration evidence, batches 1–78 and closeout audit
 
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
 ## Diagnostic binding correction evidence seal 78
 
 [Seal 78](evidence-v78.json) retains **89 members / 3,534,831 raw bytes**,
@@ -13,7 +33,7 @@ paths/symlinks, verify archive/member hashes and stop on unexplained conflicts.
 The v73 log correction remains preserved. Post-seal documentation and delivery
 readback are supplemental. **Phase 22 remains incomplete.**
 
-## Current v50 profile binding failure: stopped and closed
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/artifacts/phase22/integration/progress.md b/artifacts/phase22/integration/progress.md
index 40eb9d0..6589c92 100644
--- a/artifacts/phase22/integration/progress.md
+++ b/artifacts/phase22/integration/progress.md
@@ -1,6 +1,26 @@
 # Integration progress — not technical acceptance
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/artifacts/phase22/integration/requirements.md b/artifacts/phase22/integration/requirements.md
index ef08c20..bc2d164 100644
--- a/artifacts/phase22/integration/requirements.md
+++ b/artifacts/phase22/integration/requirements.md
@@ -1,6 +1,26 @@
 # Consolidated Phase 22 requirement checklist
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/docs/phase22-corpus-review.md b/docs/phase22-corpus-review.md
index b35e991..fb51ce7 100644
--- a/docs/phase22-corpus-review.md
+++ b/docs/phase22-corpus-review.md
@@ -1,6 +1,26 @@
 # Phase 22 independent corpus freeze packet
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/docs/phase22-implementation-status.md b/docs/phase22-implementation-status.md
index e740b8e..5413f0e 100644
--- a/docs/phase22-implementation-status.md
+++ b/docs/phase22-implementation-status.md
@@ -1,6 +1,26 @@
 # Phase 22 implementation status
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/docs/phase22-ssrf-follow-up.md b/docs/phase22-ssrf-follow-up.md
index 3a9924e..5acf27d 100644
--- a/docs/phase22-ssrf-follow-up.md
+++ b/docs/phase22-ssrf-follow-up.md
@@ -1,6 +1,26 @@
 # Replacement SSRF follow-up
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/docs/phase22-technical.md b/docs/phase22-technical.md
index 5b97b31..f13f75f 100644
--- a/docs/phase22-technical.md
+++ b/docs/phase22-technical.md
@@ -1,6 +1,26 @@
 # Phase 22 technical expansion
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/docs/phase22-timeout-policy.md b/docs/phase22-timeout-policy.md
index 850948a..da362d2 100644
--- a/docs/phase22-timeout-policy.md
+++ b/docs/phase22-timeout-policy.md
@@ -1,6 +1,26 @@
 # Phase 22 static timing policy
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
diff --git a/docs/rules.md b/docs/rules.md
index 0fc2bb6..eed11d2 100644
--- a/docs/rules.md
+++ b/docs/rules.md
@@ -1,6 +1,26 @@
 # PortunusMCP Sentinel rule catalog
 
-## Current v50 profile binding failure: stopped and closed
+## Current v51 bound profile: source-only decision pending
+
+The approved corrected profile at frozen `6e4fd67` retains **four verified worker
+identities and 79,307 partial samples**. Its whole input timed out at 300 seconds;
+cleanup passed, no report exists, and the one-use budget is closed. Shared
+TypeScript branch merge is 43.02–44.46% inclusive sample share; its missing-branch
+predicate is 8.40–9.08% leaf share. These are not speedup or removable-cost claims.
+The [review packet](https://github.com/BashaarJavaid/MCP-Sentinel/blob/phase22/integration/artifacts/phase22/integration/v51-faf-bound-sampling/summary.md) proposes **one unapproved source-only optimization**:
+recognize eligible all-present equal branch values before fallback preparation.
+No implementation, corpus/profile/retry, resource change or paid call is authorized.
+The invalid old-root profile and V49 preparation failure remain preserved; this
+passes current diagnostic attribution only. Original native/fresh failures remain.
+Product retains tested `6e4fd67`: 2,384 tests / 36 skips locally and in all 12 hosted
+suites, 29 normal jobs and docs passed; combined coverage 89.98%, branch-only
+85.85%. All **265 requirements** remain. **Phase 22 is incomplete**; explicit
+human acceptance is separate. Git stays 312/1,040 incomplete, paid benchmark and
+pilots deferred, Phase 21 incomplete and Phase 24/15 unchanged. No merge,
+ready-state, release, outreach or Phase 23.
+
+
+## Historical v50 profile binding failure: stopped and closed
 
 The approved single profile was deliberately stopped when the reused sampler's
 worker import root was found to be `17b4784` instead of approved `6e4fd67`.
